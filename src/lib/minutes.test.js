import test from'node:test';import assert from'node:assert/strict';import{createMinutes,extractKeywords,splitSentences}from'./minutes.js';test('日本語文を分割する',()=>assert.equal(splitSentences('一つ目です。二つ目です！').length,2));test('決定事項と行動を抽出する',()=>{const m=createMinutes('受付は二名にすることに決定しました。田中さんが金曜までに資料を作成します。','定例会');assert.equal(m.title,'定例会');assert.equal(m.decisions.length,1);assert.equal(m.actions.length,1)});test('キーワード数を制限する',()=>assert.equal(extractKeywords('文化祭 文化祭 受付 受付 資料',2).length,2));
