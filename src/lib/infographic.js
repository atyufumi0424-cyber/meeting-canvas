function wrap(c,text,x,y,max,line=34,maxLines=3){let row='',count=0;for(const char of text){if(c.measureText(row+char).width>max){c.fillText(row,x,y+count*line);row=char;if(++count>=maxLines)return y+count*line}else row+=char}if(row&&count<maxLines){c.fillText(row,x,y+count*line);count++}return y+count*line}
function panel(c,title,items,x,y,w,h,color){c.fillStyle='#fff';c.beginPath();c.roundRect(x,y,w,h,28);c.fill();c.fillStyle=color;c.font='700 30px system-ui';c.fillText(title,x+34,y+52);c.font='25px system-ui';let cursor=y+98;(items.length?items:['該当なし']).slice(0,3).forEach(item=>{c.fillStyle=color;c.fillText('●',x+34,cursor);c.fillStyle='#243047';cursor=wrap(c,item,x+66,cursor,w-100,32,2)+22})}
export function downloadInfographic(m){const canvas=document.createElement('canvas');canvas.width=1400;canvas.height=1050;const c=canvas.getContext('2d'),g=c.createLinearGradient(0,0,1400,1050);g.addColorStop(0,'#eef2ff');g.addColorStop(1,'#e5fbf4');c.fillStyle=g;c.fillRect(0,0,1400,1050);c.fillStyle='#101827';c.font='800 58px system-ui';wrap(c,m.title,80,100,1240,68,2);c.fillStyle='#65708a';c.font='24px system-ui';c.fillText(`MEETING CANVAS  •  ${new Date().toLocaleDateString('ja-JP')}`,80,205);panel(c,'要点',m.summary,70,250,610,335,'#5b5ce2');panel(c,'決定事項',m.decisions,720,250,610,335,'#ea5a47');panel(c,'次にやること',m.actions,70,625,820,340,'#079b83');panel(c,'キーワード',m.keywords.map(x=>`# ${x}`),930,625,400,340,'#d58a08');const a=document.createElement('a');a.download=`${m.title}-図解.png`;a.href=canvas.toDataURL('image/png');a.click()}
